source ~/hi/cli/shell/zsh/zshrc
source ~/hi/cli/shell/shell_aliases
alias core="coredns -conf Corefile"
alias cores="coredns -conf Corefile.snapp"
echo "Hi!" # to make sure the file is sourced
#export SHELL=bash  #necessary because we call another shell in screen  #will break my phook
export PATH=$(brew --prefix)/opt/findutils/libexec/gnubin:/Users/mm/Library/Python/3.8/bin:$PATH 

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
